services:
  chatbot:
    image: ghcr.io/suebi76/1zu1:latest
    build: .
    ports:
      - "8080:80"
    volumes:
      - chatbot-config:/var/www/html/config
      - chatbot-rag:/var/www/html/rag
    restart: unless-stopped

volumes:
  chatbot-config:
  chatbot-rag:
